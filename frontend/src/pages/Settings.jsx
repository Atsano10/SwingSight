// Settings page for the logged-in user
// Use the useAuth() hook to get the current user and display their info (e.g. email)
// This is where you'd add things like change password, display name, preferences, etc. later


export default function Settings(){
    return(
        <main>
            
        </main>
    )
}
